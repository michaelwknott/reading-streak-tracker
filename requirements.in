# After making changes run the following commands from the repo root to install the new packages:
# pip-compile requirements.in
# pip-compile requirements-dev.in
# pip install requirements.txt
# pip install requirements-dev.txt
# All in one:
# pip-compile requirements.in && pip-compile requirements-dev.in && pip install -r requirements.txt && pip install -r requirements-dev.txt
django>=4.2,<5
django-allauth
PyYAML # For fixtures since YAML allows for comments
django-browser-reload
crispy-tailwind # for pretty looking forms
environs[django] # for environment variables
psycopg2-binary # for postgres
gunicorn # for running production server
